# CLAUDE.md

このリポジトリは、プライベート用途のWebページをまとめて管理するためのリポジトリです。

## リポジトリの目的

用途ごとにディレクトリを分けて、さまざまな静的HTMLページを整理・保管します。

## ディレクトリ構成（方針）

```
/
├── CLAUDE.md
├── index.html          # トップページ（全コンテンツへのナビゲーション）
├── travel/             # 旅行関連ページ
├── memo/               # メモ・覚書ページ
└── tools/              # 便利ツール系ページ
```

ディレクトリは用途が増えるたびに追加していく。

## 開発ガイドライン

- 各ページは基本的に静的HTML（CSS・JSはインラインまたは同一ディレクトリに配置）
- `index.html` はトップページとして、各コンテンツへのリンクをまとめる
- ファイル名はわかりやすい英語小文字＋ハイフン区切り（例: `travel-singapore.html`）
- 外部依存は最小限に抑える（CDN利用は可、npmビルドは不要）

## コーディングスタイル

- HTML5 準拠
- インデントはスペース2つ
- 文字コードは UTF-8
